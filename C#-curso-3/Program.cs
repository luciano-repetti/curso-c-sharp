Console.WriteLine("Seleccione un componente con numero 1 - 4");
var componentesDisponibles = Enum.GetValues(typeof(TipoComponente));
// var seleccion

// for (int i = 0; i < length; i++)
// {
    
// }